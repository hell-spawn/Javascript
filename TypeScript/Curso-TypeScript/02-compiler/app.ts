let myName: string = 'Spawn';
let myAge: number= 15;
let anything;
anything = 'Spawn'

function controlMe(isTrue: boolean, someThingElse: boolean) {
	let result: number;
	if (isTrue) {
		result = 12;
	}
	result = 40;
	return result;
}
